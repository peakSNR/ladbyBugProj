[package]
name = "monad-rust-example"
version = "0.1.0"
edition = "2021"

[dependencies]
monad = { path = "../../tools/rust_api" }
arrow = { version = "55", optional = true, default-features = false, features = [] }

[features]
arrow = ["monad/arrow", "dep:arrow"]
